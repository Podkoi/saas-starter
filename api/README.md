# API placeholders

This folder contains placeholders for serverless API routes:

- `checkout.js`: one-time Stripe Checkout / demo fallback
- `subscriptions.js`: Stripe Checkout subscription creation
- `portal.js`: Stripe customer portal session creation
- `webhook.js`: Stripe webhook handler example

Implement these on your chosen platform (Vercel, Netlify, etc.).
